<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="cc.lijingbo.androidstudy">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".bigimage.ImageLoaderMainActivity"></activity>
    <activity android:name=".bigimage.ImageLoaderSecondActivity" />
    <activity
      android:name=".paypassword.MainActivity"
      android:screenOrientation="portrait" />
    <activity
      android:name=".paypassword.Main2Activity"
      android:screenOrientation="portrait" />
    <activity android:name=".imageloader_ren.ImageLoaderMainactivity">

    </activity>
    <activity android:name=".imageloader.ImageLoaderMainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

</manifest>